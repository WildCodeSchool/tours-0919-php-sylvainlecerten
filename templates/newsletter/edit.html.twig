{% extends 'base.html.twig' %}

{% block title %}Editer un e-mail{% endblock %}

{% block body %}
    <h1>Editer une adresse e-mail</h1>

    {{ include('newsletter/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <a href="{{ path('newsletter_index') }}">Retour à la liste</a>

    {{ include('newsletter/_delete_form.html.twig') }}
{% endblock %}

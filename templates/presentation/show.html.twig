{% extends 'base.html.twig' %}

{% block title %}Voir une entrée{% endblock %}

{% block body %}
    <h1 class="presentation-show__title">Paragraphe</h1>

    <table class="presentation-show__table">
        <tbody>
            <tr>
                <th class="presentation-show__table-case">Id</th>
                <td class="presentation-show__table-case">{{ presentation.id }}</td>
            </tr>
            <tr>
                <th class="presentation-show__table-case">Title</th>
                <td class="presentation-show__table-case">{{ presentation.title }}</td>
            </tr>
            <tr>
                <th class="presentation-show__table-case">Subtitle</th>
                <td class="presentation-show__table-case">{{ presentation.subtitle }}</td>
            </tr>
            <tr>
                <th class="presentation-show__table-case">Paragraph</th>
                <td class="presentation-show__table-case">{{ presentation.paragraph }}</td>
            </tr>
        </tbody>
    </table>
    <div class="presentation-show__buttons">
        <a class="presentation-show__button" href="{{ path('presentation_index') }}">Retour</a>
        <a class="presentation-show__button" href="{{ path('presentation_edit', {'id': presentation.id}) }}">Editer</a>
    </div>
    {{ include('presentation/_delete_form.html.twig') }}
{% endblock %}

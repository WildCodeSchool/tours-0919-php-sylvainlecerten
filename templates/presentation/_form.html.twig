<div class="form-container">
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="presentation__form-btn">{{ button_label|default('Sauvegarder') }}</button>
    {{ form_end(form) }}
</div>